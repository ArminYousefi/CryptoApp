package google.yousefi.cryptoapp.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

val AppTypography = Typography(
    headlineMedium = TextStyle(                // استایل برای عنوان‌های متوسط
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Bold,          // وزن متوسط
        fontSize = 24.sp                      // اندازه فونت 24sp
    ),
    headlineSmall = TextStyle(                 // استایل برای عنوان‌های کوچک
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Bold,          // وزن متوسط
        fontSize = 24.sp                      // اندازه فونت 24sp
    ),
    titleLarge = TextStyle(                    // استایل برای عنوان‌های بزرگ
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Bold,          // وزن متوسط
        fontSize = 18.sp                      // اندازه فونت 18sp
    ),
    titleMedium = TextStyle(                   // استایل برای عنوان‌های متوسط
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Bold,          // وزن متوسط
        fontSize = 20.sp                      // اندازه فونت 20sp
    ),
    titleSmall = TextStyle(                    // استایل برای عنوان‌های کوچک
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Bold,          // وزن متوسط
        fontSize = 16.sp                      // اندازه فونت 16sp
    ),
    bodyLarge = TextStyle(                     // استایل برای متن‌های بزرگ
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Normal,        // وزن عادی
        fontSize = 16.sp                      // اندازه فونت 16sp
    ),
    bodyMedium = TextStyle(                    // استایل برای متن‌های متوسط
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Normal,        // وزن عادی
        fontSize = 16.sp                      // اندازه فونت 16sp
    ),
    bodySmall = TextStyle(                     // استایل برای متن‌های کوچک
        fontFamily = FontFamily.Default,       // خانواده فونت پیش‌فرض
        fontWeight = FontWeight.Normal,        // وزن عادی
        fontSize = 15.sp                      // اندازه فونت 15sp
    )
)
